services:
  flyup-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE_URL: ${VITE_BASE_URL}
    container_name: flyup-frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
